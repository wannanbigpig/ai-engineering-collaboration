# 变更记录

仓库使用 [VERSION](VERSION) 作为唯一发布版本来源；全部 Skill 的 `metadata.version` 必须与它一致。本记录按仓库发布版本倒序维护。

## 1.6.4 - 2026-09-16

- 默认审查报告省略缺少实际调用、契约或具体业务场景的纯假设风险；探索性加固建议按用户要求单列，不混入缺陷数量和合并结论。
- 将规则发现与业务关键词搜索分开，业务搜索排除无关Skill、评测快照和辅助副本；审查目标本身位于这些目录时仍正常读取。
- 增加 `precision-v5` 修正显式工具路径场景的评分口径：所属Skill加载不单独计失败，自然触发使用不含Skill路径的独立请求。

## 1.6.3 - 2026-09-16

- `find-lessons --json` 一次返回正文、所属元数据、计数状态及源文件行号；缺失或无效计数明确为 `null`，保留原有文本格式和只读行为。
- 增加 `precision-v4`：明确范围外行为未定义的契约对照，以及查询工具的旧记录/带计数记录对照；不替换历史难例。
- 区分 CLI 事件输出与模型工具返回的证据边界；本机诊断与补充取证单独留存，不覆盖原始评分，不把内部诊断接口加入默认流程。

## 1.6.2 - 2026-09-16

- 对审查判定顺序及三向示例进行实测；未得到改善证据，撤回本轮 code-review 正文实验，保留既有契约判断边界。
- 明确经验读取的记录边界、元数据向后归属和缺失计数语义；不扩大维护 Skill 触发，撤回复测中引出重复检索的工具推荐。
- 评测保留可恢复网络错误；进程成功且回合完整完成时不再误记执行失败，最终失败及未完成仍失败。
- 增加独立 boundary-v3：输入明确拒绝、非日期配置契约、旧经验与带计数经验正反对照；历史场景和失败保留。

## 1.6.1 - 2026-09-15

- 明确审查缺陷需同时有输入支持或可达调用依据、违反契约的行为证据；额外输入差异不直接判为确定缺陷，不从不支持输入推导必须拒绝。
- 按已知测试清单选择适用验证，已知无测试时使用相关静态检查或最小只读复现；测试未知时允许发现，不增加固定全仓清点。
- 只读复现避开缓存等隐式写入，必要时使用隔离副本；搜索从已知目标开始，保留适用隐藏规则；UI 验证建议同样受实际变更和已有状态范围约束。
- 保留 legacy 场景，增加 review-v2 契约与测试正反对照、natural-v2 无固定入口抽样；消除新版经验与澄清 fixture 的关键词噪声和约定冲突。
- 命令证据关联原始事件行号，分存输出、退出码与生命周期；正确性、授权、验证真实性、流程效率、评测有效性分别复核，缺失输出不补造为通过。

## 1.6.0 - 2026-09-15

- 保留非平凡实施的修改前最小计划，支持复用会话或文件计划；仅按项目要求或持久化需要维护 `.aitasks`，不为计划形式重复确认。
- 调试允许多个有证据的候选原因，每个实验检验一个关键假设；没有新证据时说明缺口并停止等价重试。
- CodeGraph 优先级限定为尚不明确的结构和跨模块关系；UI 与风格检查仅覆盖受影响部分并复用已有基线。
- 在 `code-review` 内增加收到反馈后的事实核验，区分成立、不成立和证据不足；按已有授权修复，相关意见合并验证。
- 根据真实试验中空测试集被误报成功的证据，明确独立审查按退出码、实际输出和覆盖范围报告验证，不将预测写成实测。
- 修正调试收尾中“复现再跑相关测试”的重叠执行歧义，合并覆盖相同用例的定向与全套验证。
- 增加隔离 fixture、真实 CLI 轨迹与完整工作区快照的成对行为评测工具；运行结果与静态校验分开报告，不将工具成功退出等同于行为通过。

## 1.5.0 - 2026-09-15

- 收窄专项触发条件，按尚未解决的行为、契约或证据缺口调度；不因跨文件、规则引用或阶段切换重复加载流程。
- 明确只读任务不写记录，已有授权跨阶段继承，独立问题可合并询问；移除静态“用户已授权”声明与强制五栏交付格式。
- 复用未变化的文件、风格基线和验证结果；排除预期失败测试的重规划，formatter 默认检查模式，测试例外按行为风险判断。
- 精简经验维护的参考读取与重复预览，对齐 `.maintenance.json` 检查日期；修复 zsh 命令示例，以及旧经验缺少元数据时无法检索的问题，保留归档与计数的写入边界。
- 补充经验检索、维护状态、命令示例的回归测试，以及独立于关键词断言的工作流行为评测场景。

## 1.4.0 - 2026-09-14

- 在现有职责边界内增加可维护性治理：变更影响分析负责新增前复用与项目风格基线检查，方案实施负责沿用现有代码和 UI 体系及变更后结构复核，测试策略覆盖关键失败路径和错误语义，代码审查细化错误分类、脱敏、项目一致性与过度拆分要求。
- 验证关卡新增本次 diff 的结构质量检查，并要求重复率、圈复杂度、覆盖率和漏洞数等量化结论提供实际工具、版本、范围与输出，禁止用模型估计冒充测量结果。
- 新增八个跨 Harness 可维护性行为评测场景和 Skill 质量契约测试；CI 改为发现并运行完整单元测试集。

## 1.3.0 - 2026-09-03

- 初始化脚本新增 Gemini CLI 支持：项目模式在 `<项目>/.gemini/skills/`、用户模式在 `~/.gemini/skills/` 为每个 Skill 建立指向 `.agents/skills/` 的相对软链接；已有受管链接或内容相同的副本视为最新，其余同名内容按冲突以退出码 `2` 终止且不写入。
- Gemini 链接创建失败时输出可执行指引并以退出码 `1` 终止；将 `__pycache__/` 编译产物移出版本控制并加入忽略规则。

## 1.2.0 - 2026-08-27

- 初始化脚本新增 --scope user，可安全将完整 Skill 组合安装到 ~/.agents/skills/，且不会改动项目文件。

## 1.1.0 - 2026-08-27

- 新增项目初始化脚本，可安全复制完整 Skill 组合、安装受管 `AGENTS.md` 区块并按条件配置 `.aitasks/` 忽略规则。
- 新增项目初始化与 Codex Custom Instructions 文档，以及标准库自动化验证。

## 1.0.0 - 2026-08-27

- 建立 10 个可组合的工程协作 Skill，并以 `ai-engineering-collaboration` 作为任务主入口。
- 提供需求定界、方案实施、影响分析、系统化调试、测试先行、验证、审查、CI 归因与 `.aitasks` 维护能力。
- 为每个 Skill 提供 Codex 可选 UI 元数据，并以 `VERSION`、本地验证和 CI 保持发布版本一致。
- 收敛发布文档为 README、验证说明与贡献指南；移除无法由仓库持续验证的外部 Harness 兼容性矩阵。
